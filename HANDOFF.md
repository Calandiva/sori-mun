# 소리문 — 작업 인수인계 (어느 PC 에서든 이 문서부터)

> 마지막 갱신: 2026-09-04 · 커밋 `b41e046` 기준
> 라이브: https://calandiva.github.io/sori-mun/ · 저장소: github.com/Calandiva/sori-mun

## 프로젝트가 무엇인가

한국어·영어를 **하나의 가역 소리 부호**로 적는 예술 프로젝트.
문장 → 소리 → (어느 말로든) 문장. 부호 채널은 음높이·화성뿐 —
길이·세기는 표현이라 사람이 흔들어도 뜻이 상하지 않는다.

## 현행 문법 (v5 — "낱말은 제 소리로 문을 연다")

    낱말 = [첫 자릿음 + 베이스 C3 + 역할 내성] [자릿음 …] [종지1(+표지 내성)] [종지2]

- **여는 화음 없음.** 낱말의 첫 자릿음이 곧 첫 소리. 베이스 C3(경계
  표지)과 역할 내성(표지+7 완전5도 … 독립어+1 단2도, 흔할수록 협화)은
  그 아래에 깔린다. 멜로디만 들으면 낱말, 화성을 함께 들으면 역할.
- **조 집합**: 장·단 {A3,B3,C#4}, 중성 {C4,C#4}. 으뜸음 = 집합의
  (번호+3×등급) 나머지 번째. 으뜸음은 울리지 않는다 —
- **종지1 여덟 닻(C4~G4)이 서로소**: 절대음 하나로 (성질, 으뜸음) 이
  유일하게 풀린다. 종지2 = 닻 − 등급 하행(5,8,9,10,6,1 — 거칢 단조증가).
- 자릿음: 으뜸음 위 성질 음계(장7진·단7진·중성6진)를 지그재그 순열로.
  자릿값이 리듬 3~5 를 빌린다(부호 아님).
- 표지 내성(종지1 아래 절대음): ko낱말49 en낱말50 ko글자51 en글자52
  대문자54 이음56. 종결 = 멜로디 없는 저음 이중음 (. 48+55 ? 48+53
  ! 48+51 … 48+49).
- 합성 시 화성은 멜로디의 0.6배 진폭.
- 상세: docs/RULES.md(규칙 전체) · docs/HOWTO.md(손 계산) ·
  docs/THEORY.md(근거) · sorimun/core/codes.py(상수의 원본)

## 검증 상태 (전부 초록)

- 글리프 17,280 전수 왕복 · 2옥타브 · 길이 흔들기 270회
- 코퍼스 90문장(ko45+en45): 문자 90/90 · WAV 90/90(파이썬) ·
  브라우저 자체 90/90 · JS합성→판독 90/90 · **상호** 되읽기 90/90
- pytest 193 · tools/verify.py · tools/verify_axes.py

재검증 순서: `python tools/verify.py && python tools/verify_axes.py &&
python -m pytest`. 브라우저 층은 세션 스크래치의 parity.js 방식 —
파이썬으로 90문장 화음(JSON) 덤프 → node 에서 data.js+encoder+decoder
로드해 자체왕복/교차 비교(전체 사전은 docs/dict/*.tsv.gz 를 feedFull).

## 화면 구성 (사용자 확정 사항 — 되돌리지 말 것)

- **index.html = 무대.** 문장 입력(2,000자)+예문 칩 14개가 맨 위,
  바로 아래 시각화 상주(팝업 금지). 트랜스포트: 재생·루퍼·잔상·궤도·
  매트릭스·이펙터·템포·WAV(드라이/이펙터 포함 선택)·볼륨·레벨미터.
- **flow.html 긴 글**: 6,000자(A4) 통째로 한 곡. 흐름 그라데이션
  (지남=가라앉음·지금=켜짐·다가옴=멀수록 어두움), 전곡 리본(클릭 시크),
  지금 낱말 카드+스파크라인. 한 장 예문 3편.
- **guide/dict/input.html**: 설명·이론 / 사전·개념 / WAV·음이름·안내형
  입력 — 상단 작은 단추로만.
- **story.html 만들기의 기록**: 논문풍, 텍스트 최소. 모든 그래프·단어집을
  페이지가 실제 코덱·사전에서 그 자리에서 계산(가짜 도식 금지 — 사용자
  명시 지시).

## 이펙터 (Hologram Microcosm 참조 — 사용자 확정)

studio.js: 그레인 엔진(AudioWorklet 6초 링버퍼; 모자이크·아르페지오·
글리치·아지랑이·터널·스트럼, 템포 동기) → 핑퐁 딜레이 → 공명 필터+LFO
→ 공간(룸~성당·빙하·피어남) → 리미터 → 볼륨. 기본 프리셋 앰비언트.
오프라인 렌더(renderWet)가 같은 회로(fxGraph)로 이펙터 포함 WAV 를 굽는다.

**잡음 방지 규칙 5 (재발 주의)**: ① 되풀 조각은 붙잡는 즉시 제 버퍼로
스냅숏 ② 그레인 창은 시간 영역 올린-코사인, 5ms 하한 ③ 그레인 합
tanh, 저역 임펄스 직류 제거 ④ 재포착·모드 전환은 즉살 금지, 자연 소멸
⑤ 모든 재생 소스는 8ms 여닫이 게인(envSource).

## 알려진 한계 / 다음에 하면 좋은 것

- MUSE 유래 개념표에 잡음 짝 잔존(쌓이↔stacked, 커버↔cover/JJ 등) —
  개념표 청소 패스가 다음 큰 개선. 근사표는 손질 계속(approx_curated.tsv).
- 복문·비교급은 자리로 환원되며 단순해진다(설계상 한계, 문서화됨).
- 브라우저 한국어 가르기는 kiwi 보다 거칢 — 안 갈리면 받아적기(정확성은
  유지). 교차 인코더 화음 동치는 61/90(불필요 — 상호 되읽기가 기준).

## 빌드 사슬

자료 재빌드: tools/build_concepts.py → build_dictionary.py(필요시) →
**build_site.py**(data.js — codes 상수 바뀌면 반드시). 예제:
examples/ 는 파이썬으로 재생성. 배포 = main 푸시(GitHub Pages, ~1분).

## 사용자 취향 (세션 메모리와 동일)

오래 걸려도 제대로, 전수 검증, 한계는 솔직히. 자리 비운 동안 커밋까지.
문구는 담백하게("너무 AI같아" 금지). 시각은 선·도식·실측 데이터.
